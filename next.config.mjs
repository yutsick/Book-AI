/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: false, // Використовуйте вбудовану оптимізацію зображень
  },
};

export default nextConfig;
